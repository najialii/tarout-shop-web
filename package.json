{
  "name": "tarout-shop-web",
  "version": "1.0.0",
  "description": "Minimal e-commerce storefront (Vite + vanilla JS).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
